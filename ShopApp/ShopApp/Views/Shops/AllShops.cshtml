@model List<Shop>

<table class="table">
    <tr>
        <th>Id</th>
        <th>Shop</th>
    </tr>
    @foreach(var shop in @Model)
    {
        <tr>
            <td>@shop.Id</td>
            <td>@Html.ActionLink(@shop.Name, "ToShop", "Shops", shop)</td>
        </tr>
    }
</table>