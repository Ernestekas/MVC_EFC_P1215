@model Shop

<table>
    <tr>
        <th>Name</th>
    </tr>
</table>
@using (Html.BeginForm("Add", "Shops", FormMethod.Post))
{
    <tr>
        <td>@Html.TextBoxFor(s => s.Name)</td>
    </tr>
    
    <input type = "submit" value = "submit" />
    @Html.ValidationSummary(false)
}